---
name: test 
description: Additional info about testing in the project. Use when "write test"
---
# Test

## Code guideline
- Unit (Vitest + RTL) => write `*.test.tsx` next to the component. 
- E2E (Playwright) => write to `tests/e2e/`
